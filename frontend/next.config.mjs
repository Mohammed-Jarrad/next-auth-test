/** @type {import('next').NextConfig} */
const nextConfig = {
    // for images
    images: {
        domains: ["res.cloudinary.com"],
    },
    reactStrictMode: false,
};

export default nextConfig;
