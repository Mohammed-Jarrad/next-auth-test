import React from "react";

const SubmitButton = () => {
    return (
        <button className="btn mt-4 bg-amber-600 hover:bg-amber-700" type="submit">
            Submit
        </button>
    );
};

export default SubmitButton;
